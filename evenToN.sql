SET SERVEROUTPUT ON;
SET VERIFY OFF;

ACCEPT num NUMBER PROMPT 'ENTER A NUMBER TO CHECK EVENS TO THAT';

DECLARE

n NUMBER := &num;

BEGIN

FOR i IN 1..n LOOP
IF (i mod 2 != 0) THEN
DBMS_OUTPUT.PUT_LINE('NUMBER '||i||'is odd');

END IF;
END LOOP;

END;
/